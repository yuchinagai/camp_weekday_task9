<h1>非表示タスク一覧</h1>
<ul>
  <% @task.each do |task| %>
    <li>
      <%= link_to task.title, task%>
      <%= task.created_at.strftime("%Y年%m月%d日") %>
    </li>
  <% end %>
</ul>
<%= link_to "一覧", tasks_path %>
